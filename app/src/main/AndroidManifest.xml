<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.jonathan.proyectofinal">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_isotipo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_isotipo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.HealthProfessionalActivity">
        </activity>
        <activity android:name=".ui.MainCarer" >
        </activity>
        <activity android:name=".fragments.admin.AdminHome" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.PatientsList" >
        </activity>
        <activity android:name=".ui.Login">

            <!-- cambiar al login -->
        </activity>
        <activity android:name=".ui.LoadBrainmher"></activity>
        <activity android:name=".ui.MainActivity" /></application>

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>
</manifest>