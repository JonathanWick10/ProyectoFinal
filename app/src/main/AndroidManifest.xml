<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.jonathan.proyectofinal">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_isotipo"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_isotipo"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.Registration_Carer"></activity>
        <activity
            android:name=".ui.Games"
            android:label="@string/title_activity_games"
            android:screenOrientation="portrait"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity
            android:name="com.facebook.CustomTabActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />

                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />

                <data android:scheme="@string/fb_login_protocol_scheme" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="com.facebook.sdk.ApplicationId"
            android:value="@string/facebook_app_id"
            tools:replace="android:value" />

        <activity android:name=".fragments.admin.AdminHome" />
        <activity android:name=".ui.MainCarer"></activity>
        <activity
            android:name=".ui.MainPatient"
            android:screenOrientation="portrait"></activity>
        <activity android:name=".ui.PatientsList"></activity>
        <activity android:name=".ui.Login">

            <!-- cambiar al login -->
        </activity>
        <activity android:name=".ui.HealthProfessionalActivity"></activity>
        <activity android:name=".ui.LoadBrainmher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" />

        <receiver android:name=".data.AlertReceiver" />
    </application>

</manifest>